%p#notice= notice

%h1= @user.name
%h2= @user.email

%h3 Your Indices:
%table.table.table-condensed.table-striped
  %thead
    %tr
      %th Community
      %th Actions
  %tbody
    - Index.user_indices(current_user).each do |index|
      %tr
        %td
          %b= index.community.name
        %td
          - if can? :update, index
            = link_to 'Edit', edit_index_path(index), class: 'btn btn-default btn-sm'
            = link_to 'Delete', index_path(index), method: :delete, data: { confirm: 'Are you sure? The index can not be restored if it is deleted!' }, class: 'btn btn-danger btn-sm'
          - else 
            %b Published
